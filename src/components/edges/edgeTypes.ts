import { SyncEdge } from './SyncEdge'
import { AsyncEdge } from './AsyncEdge'
import { BidirectionalEdge } from './BidirectionalEdge'

export const edgeTypes = { sync: SyncEdge, async: AsyncEdge, bidirectional: BidirectionalEdge }
