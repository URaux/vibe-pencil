# 1. Title suggestion (~70 char)

Brainstorm per-backend prompt, batched submissions, and chat UX polish

# 2. Summary (2-3 bullets — the headline changes)

- Scoped to `feat/brainstorm-per-backend-prompt` at HEAD `a3354b7e4a96c4adeacbad0baee656086c2edd64`; Phase 1 cleanup baseline stays in `.planning/phase1/PR-DRAFT-2026-04-15.md` and is not re-described here.
- Reworks the brainstorm/chat flow around per-backend prompting, v2 scaffold wiring, batched OptionCards submissions, live-stage drafts, auto-advance behavior, and the inter-turn status marker that keeps multi-step selection flows in one chat arc.
- Polishes the chat UI and runner edge cases across `src/components/ChatPanel.tsx`, `src/components/OptionCards.tsx`, `src/app/api/chat/route.ts`, and `src/lib/agent-runner.ts`; branch footprint vs `master` is 12 files changed, 989 insertions, 218 deletions.

# 3. Commits in logical order with 1-line hook each

Current branch head: `a3354b7e4a96c4adeacbad0baee656086c2edd64`

- `83ef01c` Isolate spawned Claude Code processes from MCP/Telegram plugin interference.
- `eaee984` Follow-up subprocess isolation for the Claude Code child-process race path.
- `485a3ee` Move the server-side brainstorm system prompt to the v2 WHAT/HOW/DEPS shape with `json:user-choice`.
- `fd17eb7` Force Claude Code skill invocation on brainstorm turn 1 so the v2 scaffold actually engages.
- `00b6051` Strip v2 control comments out of visible chat output instead of leaking protocol text to the UI.
- `e6b6e1d` Land the per-backend brainstorm v2 prompt/scaffold direction and HTML-comment stripping fix.
- `33f60a6` Add the data-driven lean per-backend prompt path that this branch is centered on.
- `2e3ff64` Add `/api/chat` and persistent-agent lifecycle logging to debug the multi-stage flow.
- `7c27c5f` Fix per-message delta tracking for Claude Code stream-json text blocks.
- `538b3e4` Tighten bubble sizing and add carousel navigation plus stronger defaults for multi/ordered cards.
- `24e4f6e` Fix Windows-specific codex argv splitting and GBK stderr mojibake in chat execution.
- `c99267d` Fix card overflow, hide the carousel scrollbar, and switch to percentage-based bubble width caps.
- `4d31ed2` Batch multi-card brainstorm submissions into one `/api/chat` request.
- `08ef4f6` Add the WeChat-style status marker between turns so batched submits read as one flow.
- `de2c5bf` Fix stale reload spinner state and clean up carousel page-scroll/card-width behavior.
- `d18baec` Use `items-start` so carousel cards size to their own content instead of stretching.
- `344189e` Route single-select cards through the same batch pipeline and remove the scroll carousel path.
- `0cd8176` Add live-stage drafts, auto-advance, and default-on custom input for faster brainstorm turns.
- `838f0f0` Fix Windows `ENOENT` handling in `agent-runner` for Codex execution.
- `a3354b7` Final MED-finding cleanup pass on the brainstorm branch before drafting review.

# 4. What reviewers should focus on (testing, manual retest paths)

- Review `src/app/api/chat/route.ts`, `src/lib/context-engine.ts`, and `src/lib/cc-native-scaffold.ts` together to confirm the per-backend prompt path still matches the brainstorm v2 contract described in the 2026-04-15 draft.
- Manually retest a live brainstorm session that hits single-select, multi-select, ordered, and custom-input cards in sequence, then confirm the batched submit path stays in one conversational arc with the status marker and auto-advance behavior.
- Manually retest `src/components/ChatPanel.tsx` carousel/bubble behavior for overflow, arrow navigation, page scroll, width caps, and content-height sizing after reload.
- On Windows, verify the Codex runner path in `src/lib/agent-runner.ts` no longer trips `ENOENT` or stderr encoding issues.
- Sanity-check the touched tests (`tests/api/chat-route.test.ts`, `tests/lib/session-storage.test.ts`) against the current behavior; I did not pull fresh pass/fail output for today's branch-only commits beyond what is already recorded in `.planning/phase1/PROGRESS.md`.

# 5. Known tradeoffs / deferred work

- This draft stops at current HEAD `a3354b7e4a96c4adeacbad0baee656086c2edd64`; uncommitted work from the parallel UX-bug-fix agent is intentionally excluded.
- The older Phase 1 cleanup branch content remains documented in `.planning/phase1/PR-DRAFT-2026-04-15.md`; keeping that separate avoids re-mixing W1/W2 cleanup scope into this brainstorm-focused draft.
- Manual browser QA is still the main risk area for this branch because the newest commits are concentrated in chat flow orchestration and UI behavior rather than isolated library code.

# 6. Open questions for reviewer

- Should this land as a separate PR on top of `master`, as a stacked PR after `phase1-w1-cleanup`, or as a squash/merge of both branches once the sequence is settled?
- Is the current review scope better framed as “brainstorm prompt + submission flow” with the Windows runner fixes included, or should the runner/process hardening be peeled into a narrower follow-up PR?
- Does the removal of the old scroll-carousel path in favor of the unified batch pipeline read as a net simplification, or do reviewers want that behavior split into smaller commits before merge?
